# Software Architecture & Performance Audit Questionnaire

**System:** _<!-- product name -->_
**Prepared by:** _<!-- your name -->_
**Date started:** _<!-- YYYY-MM-DD -->_
**Audit scope:** Flutter mobile application + Railway-hosted backend
**Status:** ☐ Not started ☐ In progress ☐ Complete

---

## How to use this document

1. Fill every section. If something does not exist in your system, write **`N/A — does not exist`** rather than leaving it blank. A blank is ambiguous; an explicit "does not exist" is a finding.
2. Paste real code, not descriptions of code. Where a snippet is requested, paste it verbatim including imports and comments.
3. Redact secrets. Replace values with `<REDACTED>` but **keep the key names** — the set of configured keys is itself information.
4. Where a screenshot is requested, save it under `docs/audit/` and link it: `![alt](docs/audit/filename.png)`.
5. Do not summarize logs. Paste raw output.

**Completion tracker**

| # | Section | Owner | Status |
|---|---------|-------|--------|
| 1 | Project Overview | | ☐ |
| 2 | Repository Structure | | ☐ |
| 3 | Technology Stack | | ☐ |
| 4 | Infrastructure & Deployment Topology | | ☐ |
| 5 | Backend Architecture | | ☐ |
| 6 | API Architecture | | ☐ |
| 7 | Authentication & Authorization | | ☐ |
| 8 | Middleware Pipeline | | ☐ |
| 9 | Database Architecture | | ☐ |
| 10 | Caching Strategy | | ☐ |
| 11 | Background Jobs & Queues | | ☐ |
| 12 | File Storage & Media | | ☐ |
| 13 | Flutter Application Architecture | | ☐ |
| 14 | Flutter Network Layer | | ☐ |
| 15 | Flutter Startup & Navigation | | ☐ |
| 16 | Flutter Rendering & Data Loading | | ☐ |
| 17 | Firebase Integration | | ☐ |
| 18 | Push Notifications | | ☐ |
| 19 | Third-Party Integrations | | ☐ |
| 20 | Configuration & Environment | | ☐ |
| 21 | Build Configuration | | ☐ |
| 22 | CI/CD Pipeline | | ☐ |
| 23 | Observability: Logging, Errors, Monitoring | | ☐ |
| 24 | Security Implementation | | ☐ |
| 25 | Testing Strategy | | ☐ |
| 26 | Performance Evidence & Measurements | | ☐ |
| 27 | Known Constraints & Pain Points | | ☐ |
| 28 | Appendices | | ☐ |

---

# 1. Project Overview

### 1.1 Product description

> What the application does, in 3–5 sentences. Written for an engineer who has never seen it.

```
<!-- paste -->
```

### 1.2 Core user journeys

List the flows that matter most, in order of business importance.

| # | Journey | Entry point (screen) | Backend endpoints involved | Frequency |
|---|---------|---------------------|---------------------------|-----------|
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |

### 1.3 Scale & usage

| Metric | Current value | Peak observed | Source of number |
|---|---|---|---|
| Registered users | | | |
| Daily active users | | | |
| Peak concurrent users | | | |
| Requests per minute (avg) | | | |
| Requests per minute (peak) | | | |
| Largest table row count | | | |
| Total DB size | | | |
| Daily new records (approx) | | | |

> If any number is a guess, mark it `(estimate)`.

### 1.4 Team & history

| Question | Answer |
|---|---|
| How many engineers work on this? | |
| How long has it been in production? | |
| Was any part generated by AI / scaffolded / inherited from a template? | |
| Has there been a major rewrite or migration? | |
| Are there parts of the codebase nobody currently understands? | |

### 1.5 Environments

| Environment | Exists? | URL / host | Purpose | Data source |
|---|---|---|---|---|
| Local dev | ☐ | | | |
| Staging | ☐ | | | |
| Production | ☐ | | | |
| Other | ☐ | | | |

---

# 2. Repository Structure

### 2.1 Repository layout

| Question | Answer |
|---|---|
| Monorepo or multi-repo? | |
| Repository URL(s) | |
| Default branch | |
| Branching model (trunk / gitflow / other) | |
| Are backend and app versioned together? | |

### 2.2 Top-level tree

> Run and paste: `git ls-files | head -300` — and separately, for each repo not in this workspace.

```
<!-- paste -->
```

### 2.3 Directory purpose map

| Path | Purpose | Owner |
|---|---|---|
| | | |

### 2.4 Repository hygiene

> Paste `.gitignore` for each repo. Also answer:

| Question | Answer |
|---|---|
| Are build artifacts committed? | |
| Are `.env` files committed? | |
| Are lockfiles committed (`pubspec.lock`, `package-lock.json`, etc.)? | |
| Are generated files (`*.g.dart`, `*.freezed.dart`) committed? | |
| Repository size (`git count-objects -vH`) | |
| Any large binaries in history? | |

```
<!-- paste .gitignore -->
```

---

# 3. Technology Stack

### 3.1 Backend

| Item | Value |
|---|---|
| Language | |
| Language version (local) | |
| Language version (production runtime) | |
| Framework + version | |
| Package manager | |
| Process manager (if any) | |
| Build step required? | |
| TypeScript? Strict mode? | |

### 3.2 Frontend (Flutter)

> Paste full output of `flutter --version` and `flutter doctor -v`.

```
<!-- paste -->
```

| Item | Value |
|---|---|
| Flutter channel | |
| Dart SDK version | |
| Minimum Android SDK | |
| Target Android SDK | |
| Minimum iOS version | |
| Null safety fully migrated? | |

### 3.3 Dependency manifests

> Paste each file **verbatim and unedited**.

**Backend manifest** (`package.json` / `requirements.txt` / `pyproject.toml` / `go.mod`)

```
<!-- paste -->
```

**Flutter** (`pubspec.yaml`)

```yaml
<!-- paste -->
```

**Flutter lockfile** — paste `pubspec.lock`, or at minimum the output of `flutter pub deps --style=compact`

```
<!-- paste -->
```

### 3.4 Dependency audit

| Question | Answer |
|---|---|
| Output of `flutter pub outdated` | (paste below) |
| Output of backend outdated check (`npm outdated` / `pip list --outdated`) | (paste below) |
| Any packages pinned to a specific version, and why? | |
| Any forked or git-referenced dependencies? | |
| Any dependencies no longer maintained? | |

```
<!-- paste outdated output -->
```

### 3.5 Stack summary table

| Layer | Technology | Version | Managed by | Notes |
|---|---|---|---|---|
| Mobile client | Flutter | | | |
| Backend runtime | | | | |
| Web framework | | | | |
| Database | | | | |
| ORM / query layer | | | | |
| Cache | | | | |
| Queue | | | | |
| Object storage | | | | |
| CDN | | | | |
| Auth provider | | | | |
| Push | | | | |
| Analytics | | | | |
| Crash reporting | | | | |
| Hosting | Railway | | | |

---

# 4. Infrastructure & Deployment Topology

### 4.1 Architecture diagram

> Attach or draw the current topology: client → edge → API → data stores → external services. Hand-drawn is acceptable. If none exists, say so.

![topology](docs/audit/topology.png)

### 4.2 Railway project

> **Screenshot required:** the Railway project canvas showing all services and their links.

![railway-canvas](docs/audit/railway-canvas.png)

| Service name | Type | Purpose | Region | Plan / resources | Replicas |
|---|---|---|---|---|---|
| | | | | | |
| | | | | | |

### 4.3 Service configuration (per service)

> Repeat this block for each Railway service.

**Service:** `<name>`

| Setting | Value |
|---|---|
| Builder (Nixpacks / Dockerfile / Buildpack) | |
| Start command | |
| Health check path | |
| Health check timeout | |
| Restart policy | |
| Sleep / autoscaling enabled? | |
| Memory limit | |
| vCPU limit | |
| Public domain | |
| Private networking used? | |

**Screenshots required:** Settings tab, Variables tab (values redacted), Metrics tab (last 7 days: CPU, memory, network).

![railway-settings](docs/audit/railway-settings.png)
![railway-metrics](docs/audit/railway-metrics.png)

### 4.4 Docker configuration

> Paste `Dockerfile`, `.dockerignore`, `docker-compose.yml`, `railway.json` / `railway.toml`, `nixpacks.toml` — whichever exist.

```dockerfile
<!-- Dockerfile -->
```

```
<!-- .dockerignore -->
```

```toml
<!-- railway.toml / nixpacks.toml -->
```

| Question | Answer |
|---|---|
| Base image and tag | |
| Multi-stage build? | |
| Final image size | |
| Build duration | |
| Does the container run as root? | |
| Is the health check defined in Docker or Railway? | |

### 4.5 Networking & geography

| Item | Region / location |
|---|---|
| API service region | |
| Database region | |
| Redis region (if any) | |
| Object storage region | |
| Firebase project location | |
| CDN edge coverage | |
| Primary user population | |
| Secondary user populations | |

| Question | Answer |
|---|---|
| Is the app talking to the API over a public Railway domain or a custom domain? | |
| Is there a proxy / API gateway / Cloudflare in front? | |
| Do services communicate over Railway private network or public internet? | |
| Is HTTP/2 enabled end-to-end? | |
| Any known egress or bandwidth limits? | |

### 4.6 Deployment process

| Question | Answer |
|---|---|
| How is a deploy triggered? | |
| Deploy duration (build + release) | |
| Zero-downtime or restart? | |
| Rollback procedure | |
| Deploy frequency | |
| Who can deploy? | |
| Are migrations run on deploy? How? | |

---

# 5. Backend Architecture

### 5.1 Architectural style

| Question | Answer |
|---|---|
| Monolith / modular monolith / microservices? | |
| Layering convention (controller → service → repository?) | |
| Is the layering enforced or aspirational? | |
| Any DDD / clean-architecture conventions? | |
| Where does business logic actually live? | |

### 5.2 Backend folder structure

> Paste `tree -L 3 -I 'node_modules|.git|dist|build|__pycache__'` from the backend root.

```
<!-- paste -->
```

| Directory | Responsibility | File count |
|---|---|---|
| | | |

### 5.3 Entry point

> Paste the server entry file in full (`index.js` / `server.ts` / `main.py` / `app.py` / `main.go`), including all `app.use(...)` / middleware registration and the listen call.

```
<!-- paste -->
```

| Question | Answer |
|---|---|
| What runs before the server starts listening? | |
| Are DB connections established at boot or lazily? | |
| Is anything loaded into memory at boot (config, catalogs, models)? | |
| How long does the process take from start to "ready"? | |
| Is there a graceful shutdown handler? | |

### 5.4 Application bootstrap order

> Numbered list of what happens on process start, in exact order.

```
1.
2.
3.
```

### 5.5 Dependency injection

| Question | Answer |
|---|---|
| Is a DI container used? Which? | |
| Or are modules imported directly / singletons? | |
| Are services instantiated per-request or once? | |
| Are DB clients singletons? |
| Any circular dependency issues known? | |

```
<!-- paste DI/container setup, or the module wiring code -->
```

---

# 6. API Architecture

### 6.1 API style

| Question | Answer |
|---|---|
| REST / GraphQL / RPC / mixed? | |
| Versioning scheme (`/v1`, header, none) | |
| Base URL(s) | |
| Content type(s) | |
| Is there an OpenAPI/Swagger spec? Where? | |
| Is the spec generated or hand-written? | |
| Is it current? | |

### 6.2 Complete endpoint inventory

> Every endpoint. Add rows as needed. This table is the backbone of the audit — do not abbreviate it.

| Method | Path | Purpose | Auth required | Roles | Middleware chain | DB queries issued | External calls | Cached? | Paginated? | Typical p50 latency | Typical p95 latency |
|---|---|---|---|---|---|---|---|---|---|---|---|
| | | | | | | | | | | | |

### 6.3 Route registration

> Paste the router/route registration files (all of them, or the index that mounts them).

```
<!-- paste -->
```

### 6.4 Representative route implementations

> Paste the **full handler source** — controller, service, and repository layers — for the following, so the complete path from HTTP to SQL is visible.

**6.4.1 The slowest endpoint you are aware of**

Path: `<!-- -->`

```
<!-- paste full chain -->
```

**6.4.2 The most frequently called endpoint**

Path: `<!-- -->`

```
<!-- paste full chain -->
```

**6.4.3 The home / dashboard endpoint (the one the app hits on launch)**

Path: `<!-- -->`

```
<!-- paste full chain -->
```

**6.4.4 A list endpoint (products, orders, or logs)**

Path: `<!-- -->`

```
<!-- paste full chain -->
```

**6.4.5 A write endpoint (order placement / subscription update)**

Path: `<!-- -->`

```
<!-- paste full chain -->
```

### 6.5 Request/response contracts

For each of the five endpoints above, paste a **real** request and response.

```http
<!-- request: method, path, headers (redacted), body -->
```

```json
<!-- response: status, headers, full body -->
```

| Endpoint | Response body size (KB) | Number of top-level items | Nesting depth | Fields the client actually uses |
|---|---|---|---|---|
| | | | | |

### 6.6 Pagination

| Question | Answer |
|---|---|
| Which endpoints are paginated? | |
| Strategy (offset / cursor / keyset / none) | |
| Default page size | |
| Maximum page size | |
| Is a total count returned? How is it computed? | |
| Which list endpoints return unbounded results? | |

```
<!-- paste pagination implementation -->
```

### 6.7 Payload shaping

| Question | Answer |
|---|---|
| Is response compression enabled (gzip/brotli)? Where configured? | |
| Are responses serialized via DTOs or raw ORM objects? | |
| Is field selection / sparse fieldsets supported? | |
| Are related entities eagerly included by default? | |
| Are any binary blobs or base64 payloads returned in JSON? | |

---

# 7. Authentication & Authorization

### 7.1 Authentication mechanism

| Question | Answer |
|---|---|
| Method (JWT / session / Firebase Auth / OAuth / custom) | |
| Who issues the credential? | |
| Token lifetime | |
| Refresh token used? Lifetime? | |
| Where is the token stored on device? | |
| Is the token verified locally or via a network call to a provider? | |
| Is verification cached? | |

### 7.2 OTP / phone login flow

> The repo shows an OTP screen; document that flow specifically.

| Question | Answer |
|---|---|
| OTP provider | |
| Is OTP sent by the backend or by Firebase directly? | |
| OTP length, TTL, retry limit | |
| Is rate limiting applied? Where? | |
| What happens between "OTP verified" and "home screen rendered"? | |

### 7.3 End-to-end auth sequence

> Numbered, one line per network call or local operation, from app launch to authenticated home screen. Include every round trip.

```
1.
2.
3.
```

| Step | Network call? | Blocking UI? | Typical duration |
|---|---|---|---|
| | | | |

### 7.4 Auth code

> Paste in full:

**Backend: token issuance**
```
<!-- paste -->
```

**Backend: auth middleware / guard**
```
<!-- paste -->
```

**Flutter: auth provider / repository**
```dart
<!-- paste lib/providers/auth_provider.dart -->
```

**Flutter: token storage and attachment to requests**
```dart
<!-- paste -->
```

### 7.5 Session persistence

| Question | Answer |
|---|---|
| How does the app know it is logged in on cold start? | |
| Is a validation call made on every launch? | |
| What happens on 401 mid-session? | |
| Is there silent refresh? Is it queued/deduplicated? | |
| Can multiple simultaneous refreshes occur? | |

### 7.6 Authorization

| Question | Answer |
|---|---|
| Role model (roles, permissions, none) | |
| Where are authorization checks performed? | |
| Are object-level ownership checks done? Where? | |
| Are there admin-only endpoints? How are they protected? | |
| Is authorization data fetched per request? Cached? | |

```
<!-- paste authorization middleware / policy code -->
```

---

# 8. Middleware Pipeline

### 8.1 Ordered middleware list

> In exact registration order.

| # | Middleware | Global or route-scoped | What it does | Does it await I/O? | Approx cost per request |
|---|---|---|---|---|---|
| 1 | | | | | |
| 2 | | | | | |

### 8.2 Middleware source

> Paste each custom middleware in full.

```
<!-- paste -->
```

### 8.3 Cross-cutting concerns

| Concern | Implemented? | Where | Configuration |
|---|---|---|---|
| CORS | ☐ | | |
| Body parsing + size limit | ☐ | | |
| Compression | ☐ | | |
| Rate limiting | ☐ | | |
| Request logging | ☐ | | |
| Request ID / correlation ID | ☐ | | |
| Helmet / security headers | ☐ | | |
| Input validation | ☐ | | |
| Timeout enforcement | ☐ | | |
| Error handler | ☐ | | |

---

# 9. Database Architecture

### 9.1 Database platform

| Item | Value |
|---|---|
| Engine and version | |
| Hosted where | |
| Instance size / plan | |
| Storage allocated / used | |
| Max connections configured | |
| Backup schedule | |
| Read replicas? | |

### 9.2 Schema

> Paste the full schema. Any of: Prisma schema, migration files, `\d+` output, Mongoose models, SQLAlchemy models, or `pg_dump --schema-only`.

```
<!-- paste -->
```

### 9.3 Table inventory

| Table / collection | Purpose | Row count | Size | Indexes | Growth rate |
|---|---|---|---|---|---|
| | | | | | |

### 9.4 Relationships

> ER diagram if one exists, otherwise describe the key relationships and cardinalities.

![erd](docs/audit/erd.png)

```
<!-- paste -->
```

### 9.5 Indexes

> Paste actual index definitions. For Postgres: `SELECT schemaname, tablename, indexname, indexdef FROM pg_indexes WHERE schemaname='public';`

```
<!-- paste -->
```

| Table | Index | Columns | Type | Used by which query |
|---|---|---|---|---|
| | | | | |

### 9.6 ORM configuration

| Question | Answer |
|---|---|
| ORM / query builder / driver | |
| Version | |
| Is lazy loading enabled? | |
| Are relations eagerly loaded by default? | |
| Is query logging available? Enabled in prod? | |
| Are raw queries used anywhere? Where? | |

```
<!-- paste ORM initialization / config -->
```

### 9.7 Connection management

| Question | Answer |
|---|---|
| Pool minimum size | |
| Pool maximum size | |
| Idle timeout | |
| Connection acquire timeout | |
| Statement timeout | |
| Is a pooler used (PgBouncer / Prisma Accelerate / RDS Proxy)? | |
| Is the client a singleton, or created per request? | |
| Are connections released on error paths? | |
| Observed peak connection count | |

```
<!-- paste the exact connection/pool configuration code -->
```

### 9.8 Query implementations

> For each of the five representative endpoints in §6.4, paste the exact queries executed and their `EXPLAIN ANALYZE` output.

**Endpoint:** `<!-- -->`

```sql
-- query
```

```
-- EXPLAIN ANALYZE output
```

### 9.9 Query behavior audit

| Question | Answer |
|---|---|
| Are there known N+1 query patterns? Where? | |
| Are queries issued inside loops anywhere? | |
| Are there queries without `LIMIT`? | |
| Are there `SELECT *` queries on wide tables? | |
| Are transactions used? Where and how long do they stay open? | |
| Any known lock contention? | |
| Slowest queries by total time — paste `pg_stat_statements` top 20 if available | |

```
<!-- paste pg_stat_statements output -->
```

### 9.10 Migrations

| Question | Answer |
|---|---|
| Migration tool | |
| Number of migrations | |
| Are migrations run automatically on deploy? | |
| Have any migrations been edited after being applied? | |
| Is there drift between schema files and production? | |

---

# 10. Caching Strategy

> If no caching exists anywhere, state that explicitly and skip to §10.5.

### 10.1 Cache inventory

| Layer | Technology | What is cached | TTL | Invalidation trigger | Hit rate |
|---|---|---|---|---|---|
| HTTP / CDN | | | | | |
| Application in-memory | | | | | |
| Redis / external | | | | | |
| Database query cache | | | | | |
| Client-side (Flutter) | | | | | |
| Image cache | | | | | |

### 10.2 Cache implementation

```
<!-- paste cache client setup and a representative get/set site -->
```

### 10.3 HTTP caching headers

| Endpoint | `Cache-Control` | `ETag` | `Last-Modified` | Honored by client? |
|---|---|---|---|---|
| | | | | |

### 10.4 Client-side persistence

| Question | Answer |
|---|---|
| Is API data persisted locally (Hive/Isar/SQLite/SharedPreferences)? | |
| Which data? | |
| Is stale data shown while refreshing? | |
| Is there an offline mode? | |

### 10.5 If no caching

| Question | Answer |
|---|---|
| Was caching removed at some point? | |
| Is any data recomputed on every request that is effectively static? | |

---

# 11. Background Jobs & Queues

| Question | Answer |
|---|---|
| Is there a queue system? Which? | |
| Is there a separate worker process/service? | |
| Are jobs run in-process on the API? | |
| What jobs exist? | |
| Cron / scheduled tasks — what, when, where they run | |
| Retry policy | |
| Dead-letter handling | |
| Do any jobs run during a user-facing request? | |

### 11.1 Job inventory

| Job | Trigger | Schedule | Runtime | Where it runs | Failure handling |
|---|---|---|---|---|---|

### 11.2 Job code

```
<!-- paste queue/scheduler setup and one representative job -->
```

---

# 12. File Storage & Media

| Question | Answer |
|---|---|
| Storage provider | |
| Bucket(s) and region | |
| Are uploads proxied through the API or direct-to-storage? | |
| Are signed URLs used? TTL? | |
| Is a CDN in front of storage? | |
| Are images resized/transcoded? Where? When? |
| Are multiple resolutions stored? | |
| Typical image file size | |
| Largest asset served | |
| Total storage used | |

### 12.1 Upload implementation

```
<!-- paste upload handler -->
```

### 12.2 Image delivery

| Question | Answer |
|---|---|
| Full URL format of a product image | |
| Image format(s) served (JPEG/PNG/WebP/AVIF) | |
| Are dimensions constrained server-side or client-side? | |
| Is there a placeholder / blurhash / thumbnail strategy? | |

> Paste one real image URL and the output of `curl -sI '<url>'`.

```
<!-- paste -->
```

---

# 13. Flutter Application Architecture

### 13.1 Architectural pattern

| Question | Answer |
|---|---|
| Pattern (MVVM / MVC / Clean / feature-first / none) | |
| Is it applied consistently? | |
| Where does business logic live? | |
| Is there a repository layer? | |
| Are models code-generated? Tool? |

### 13.2 Folder structure

> Paste `tree lib -L 3`.

```
<!-- paste -->
```

| Directory | Responsibility |
|---|---|
| `lib/providers/` | |
| `lib/screens/` | |
| `lib/services/` | |
| `lib/widgets/` | |
| `lib/utils/` | |
| `lib/theme/` | |

### 13.3 State management

| Question | Answer |
|---|---|
| Library used (Provider / Riverpod / Bloc / GetX / setState) | |
| Is more than one used? | |
| How many top-level providers exist? | |
| Where are they registered? | |
| Are any created eagerly at app start? | |
| Are `Consumer` / `Selector` used, or whole-tree rebuilds? | |
| Is `notifyListeners()` called in loops or during build? | |
| Is any provider holding large collections in memory? | |

> Paste the provider registration block from `main.dart`, and two full providers — the largest one and the one on the home screen path.

```dart
<!-- main.dart provider registration -->
```

```dart
<!-- provider 1 -->
```

```dart
<!-- provider 2 -->
```

### 13.4 Dependency injection

| Question | Answer |
|---|---|
| DI approach (get_it / Provider / manual / none) | |
| Are singletons lazy or eager? | |
| Is the API client a singleton? | |
| How do providers obtain services? | |

```dart
<!-- paste DI setup -->
```

### 13.5 Screen inventory

| Screen | File | Providers consumed | API calls on open | Loads on app start? | Perceived speed |
|---|---|---|---|---|---|
| Splash | | | | | |
| Login | | | | | |
| OTP | | | | | |
| Home | | | | | |
| Products | | | | | |
| Product detail | | | | | |
| Cart | | | | | |
| Subscription | | | | | |
| Calendar | | | | | |
| Delivery logs | | | | | |
| Reports | | | | | |
| Notifications | | | | | |
| Livestream | | | | | |
| Profile | | | | | |

---

# 14. Flutter Network Layer

### 14.1 HTTP client

| Question | Answer |
|---|---|
| Library (`dio` / `http` / `chopper` / custom) | |
| Is a single client instance reused? | |
| Is the connection pool / keep-alive preserved? | |
| Base URL configuration | |
| Connect timeout | |
| Receive timeout | |
| Send timeout | |
| Retry policy | |
| Is gzip accepted? | |
| Are certificates pinned? | |

### 14.2 API service source

> Paste the complete API client / service layer files.

```dart
<!-- paste -->
```

### 14.3 Interceptors

| # | Interceptor | Purpose | Performs I/O? | Blocking? |
|---|---|---|---|---|

```dart
<!-- paste interceptors -->
```

### 14.4 Request behavior

| Question | Answer |
|---|---|
| Are requests deduplicated? | |
| Are parallel requests batched or fired individually? | |
| Are requests cancelled on screen dispose? | |
| Is there a request queue or concurrency limit? | |
| Is any request made on every widget rebuild? | |
| Are requests made inside `build()` anywhere? | |

### 14.5 Observed network timing

> From Flutter DevTools Network tab or Charles/Proxyman, on a real device over a normal connection.

| Endpoint | DNS | TLS | TTFB | Download | Total | Response size |
|---|---|---|---|---|---|---|
| | | | | | | |

**Screenshot required:** DevTools Network view during app launch.

![devtools-network](docs/audit/devtools-network.png)

---

# 15. Flutter Startup & Navigation

### 15.1 `main.dart`

> Paste `main()` and the root widget in full.

```dart
<!-- paste -->
```

### 15.2 Startup sequence

> Every operation from process start to first interactive frame, in order.

| # | Operation | Sync/Async | Blocks first frame? | Duration (ms) |
|---|---|---|---|---|
| 1 | | | | |
| 2 | | | | |

| Question | Answer |
|---|---|
| What is initialized before `runApp()`? | |
| Is Firebase initialized before `runApp()`? | |
| Is any network call awaited before first frame? | |
| Is any disk read awaited before first frame? | |
| Are remote config / feature flags fetched at start? | |

### 15.3 Splash screen

```dart
<!-- paste splash_screen.dart -->
```

| Question | Answer |
|---|---|
| Is there a native splash (`flutter_native_splash`)? | |
| Is there also a Dart splash screen? | |
| Is there a minimum display duration / artificial delay? | |
| What must complete before splash dismisses? | |
| How is the next route decided? | |
| How long does splash typically stay up? | |

### 15.4 Measured startup

> From `flutter run --profile --trace-startup`, paste `start_up_info.json`.

```json
<!-- paste -->
```

| Metric | Debug | Profile | Release |
|---|---|---|---|
| Time to Flutter engine init | | | |
| Time to first frame | | | |
| Time to first *useful* frame | | | |
| Time to interactive | | | |

### 15.5 Navigation

| Question | Answer |
|---|---|
| Navigator 1.0 / 2.0 / go_router / auto_route | |
| Are routes generated or inline? | |
| Are screens kept alive when navigating away? | |
| Is `IndexedStack` used for tabs? | |
| Do screens refetch on every navigation? | |
| Is there a bottom nav / curved navbar rebuilding the whole tree? | |

```dart
<!-- paste route definitions and navbar implementation -->
```

---

# 16. Flutter Rendering & Data Loading

### 16.1 List rendering

| Screen | Widget used | Builder or eager children | `itemExtent` set? | Item count | Pagination |
|---|---|---|---|---|---|
| | | | | | |

| Question | Answer |
|---|---|
| Are `ListView`/`GridView` builders used, or `Column` + `map`? | |
| Is `shrinkWrap: true` used? Where? | |
| Are nested scroll views present? | |
| Is `const` used on static widgets? | |

### 16.2 Images

| Question | Answer |
|---|---|
| Image widget used (`Image.network` / `CachedNetworkImage` / other) | |
| Is `cacheWidth`/`cacheHeight` or `ResizeImage` used? | |
| Is there a disk cache? Size limit? | |
| Are placeholders / fade-in used? | |
| Are full-resolution images shown in list thumbnails? | |
| Typical image dimensions served vs. displayed | |

```dart
<!-- paste a representative image widget usage -->
```

### 16.3 Pagination on client

| Question | Answer |
|---|---|
| Is infinite scroll implemented? Where? | |
| Trigger mechanism | |
| Page size requested | |
| Is the full list held in memory? | |

### 16.4 Performance profile

> Run in **profile mode** on a physical device. Paste/attach:

**Screenshot required:** DevTools Performance timeline during home screen scroll.

![devtools-performance](docs/audit/devtools-performance.png)

| Metric | Value |
|---|---|
| Jank frames during launch | |
| Jank frames during home scroll | |
| Worst frame build time | |
| Worst raster time | |
| Peak memory (Dart heap) | |
| Peak memory (total RSS) | |
| Widget rebuild count on home (from Rebuild Stats) | |

### 16.5 Build config used for measurement

| Question | Answer |
|---|---|
| Were the above measured in debug, profile, or release? | |
| Physical device or emulator? | |
| Device model and OS version | |
| Network conditions | |

---

# 17. Firebase Integration

| Product | Used? | Purpose | SDK version | Initialized when |
|---|---|---|---|---|
| Core | ☐ | | | |
| Auth | ☐ | | | |
| Cloud Messaging | ☐ | | | |
| Firestore | ☐ | | | |
| Realtime Database | ☐ | | | |
| Storage | ☐ | | | |
| Analytics | ☐ | | | |
| Crashlytics | ☐ | | | |
| Performance Monitoring | ☐ | | | |
| Remote Config | ☐ | | | |
| Dynamic Links | ☐ | | | |
| Cloud Functions | ☐ | | | |

| Question | Answer |
|---|---|
| Firebase project ID(s) | |
| Are separate projects used per environment? | |
| Is `google-services.json` / `GoogleService-Info.plist` committed? | |
| Is Firebase initialized on the backend too (Admin SDK)? | |
| Does the backend verify Firebase ID tokens on each request? | |
| Are there active Firestore listeners? Where? How many? | |

```dart
<!-- paste firebase initialization code -->
```

```
<!-- paste firebase_options.dart (redact API keys if you prefer) -->
```

---

# 18. Push Notifications

| Question | Answer |
|---|---|
| Provider (FCM / OneSignal / other) | |
| Who sends notifications (backend / console / third party)? | |
| Token registration flow | |
| When is the token registered/refreshed? | |
| Are tokens stored in the DB? Deduplicated? | |
| Foreground handler behavior | |
| Background handler behavior | |
| Notification tap → navigation handling | |
| Is a local notifications plugin also used? | |
| iOS APNs configuration status | |
| Do notifications trigger API calls on receipt? | |

```dart
<!-- paste lib/services/fcm_service.dart in full -->
```

```
<!-- paste backend notification sending code -->
```

---

# 19. Third-Party Integrations

| Service | Purpose | Called from | Sync/async | In request path? | Timeout | Failure behavior | Cost model |
|---|---|---|---|---|---|---|---|
| | | | | | | | |

| Question | Answer |
|---|---|
| Which third-party calls happen during a user-facing request? | |
| Are any third-party calls unbounded (no timeout)? | |
| Are responses from third parties cached? | |
| Are there webhooks inbound? Which endpoints? | |
| Are webhook signatures verified? | |

```
<!-- paste one representative third-party client implementation -->
```

---

# 20. Configuration & Environment

### 20.1 Backend environment variables

> **Key names only.** Values redacted, but indicate whether each is set in production.

| Key | Purpose | Set in prod? | Set in staging? | Set locally? |
|---|---|---|---|---|
| | | ☐ | ☐ | ☐ |

| Question | Answer |
|---|---|
| Is `NODE_ENV` / equivalent set to production? | |
| How are variables loaded (`dotenv`, Railway injection)? | |
| Is there validation of required env vars at boot? | |
| What happens if a required var is missing? | |
| Are any config values hardcoded in source? | |

### 20.2 Flutter configuration

| Question | Answer |
|---|---|
| How is the API base URL configured? | |
| Is `--dart-define` used? Which keys? | |
| Are there flavors / build variants? | |
| Is the production URL hardcoded anywhere? | |
| Are debug flags shipped in release builds? | |

```dart
<!-- paste lib/utils/constants.dart -->
```

### 20.3 Feature flags

| Question | Answer |
|---|---|
| Do feature flags exist? System used? | |
| Are they fetched at startup? Blocking? | |
| Are there dead flags still evaluated? | |

---

# 21. Build Configuration

### 21.1 Android

> Paste `android/app/build.gradle.kts`, `android/build.gradle.kts`, `gradle.properties`, and `AndroidManifest.xml`.

```kotlin
<!-- paste -->
```

| Question | Answer |
|---|---|
| minSdk / targetSdk / compileSdk | |
| Is R8 / minification enabled for release? | |
| Is resource shrinking enabled? | |
| Is `--split-per-abi` or App Bundle used? | |
| Release APK / AAB size | |
| Is ProGuard configured? Rules file? | |
| Are debug symbols stripped? | |

### 21.2 iOS

| Question | Answer |
|---|---|
| Deployment target | |
| Bitcode / dSYM settings | |
| IPA size | |
| Any custom Podfile modifications? | |

### 21.3 Build outputs

> Paste output of `flutter build apk --release --analyze-size` (or `appbundle`).

```
<!-- paste -->
```

| Asset category | Size |
|---|---|
| Dart AOT code | |
| Flutter engine | |
| Assets (images/fonts) | |
| Native libs | |
| Total | |

### 21.4 Assets

| Question | Answer |
|---|---|
| Total bundled asset size | |
| Largest bundled asset | |
| Are unused assets bundled? | |
| Are custom fonts used? How many weights? | |
| Are images pre-optimized? | |

---

# 22. CI/CD Pipeline

| Question | Answer |
|---|---|
| CI system | |
| What runs on PR? | |
| What runs on merge? | |
| Is the backend deployed automatically? | |
| Is the app built in CI or locally? | |
| Are tests required to pass before deploy? | |
| Is there a linting/analysis gate? | |
| Average pipeline duration | |

> Paste CI configuration files (`.github/workflows/*.yml`, `codemagic.yaml`, etc.).

```yaml
<!-- paste -->
```

---

# 23. Observability: Logging, Errors, Monitoring

### 23.1 Backend logging

| Question | Answer |
|---|---|
| Logging library | |
| Log format (JSON / plain) | |
| Log level in production | |
| Is every request logged? | |
| Are request bodies logged? | |
| Are response times logged? | |
| Is logging synchronous? | |
| Where do logs go beyond Railway? | |
| Retention period | |

```
<!-- paste logger configuration -->
```

### 23.2 Raw production logs

> Paste **at least 100 consecutive lines** of real production logs covering an app cold start and a home screen load. Do not filter or clean them.

```
<!-- paste -->
```

### 23.3 Error handling — backend

| Question | Answer |
|---|---|
| Is there a global error handler? | |
| Are errors typed/classified? | |
| Are stack traces returned to clients? | |
| Are unhandled rejections/exceptions caught? | |
| Do errors cause process restarts? | |
| Is there an error tracking service (Sentry etc.)? | |

```
<!-- paste global error handler -->
```

### 23.4 Error handling — Flutter

| Question | Answer |
|---|---|
| Is `FlutterError.onError` overridden? | |
| Is `runZonedGuarded` / `PlatformDispatcher.onError` used? | |
| How are API errors surfaced to users? | |
| Are errors silently swallowed anywhere (`catch {}`)? | |
| Is Crashlytics receiving non-fatal errors? | |

```dart
<!-- paste error handling setup -->
```

### 23.5 Monitoring & APM

| Capability | Implemented? | Tool | Notes |
|---|---|---|---|
| Uptime monitoring | ☐ | | |
| APM / tracing | ☐ | | |
| Distributed tracing | ☐ | | |
| DB query monitoring | ☐ | | |
| Custom metrics | ☐ | | |
| Alerting | ☐ | | |
| Real user monitoring (mobile) | ☐ | | |
| Firebase Performance traces | ☐ | | |

**Screenshots required:** Railway metrics (7 days), database metrics, Firebase Performance dashboard, Crashlytics dashboard — whichever exist.

![metrics](docs/audit/metrics.png)

### 23.6 Analytics

| Question | Answer |
|---|---|
| Analytics tool(s) | |
| Events tracked | |
| Is analytics initialized at startup? Blocking? | |
| Is event dispatch batched? | |

---

# 24. Security Implementation

| Control | Implemented? | Details |
|---|---|---|
| HTTPS enforced | ☐ | |
| HSTS | ☐ | |
| Input validation library | ☐ | |
| SQL injection protection (parameterized queries) | ☐ | |
| Rate limiting | ☐ | |
| Brute-force protection on OTP/login | ☐ | |
| CORS restricted to known origins | ☐ | |
| Secrets in env only (not in code) | ☐ | |
| Secrets rotated | ☐ | |
| Password hashing algorithm (if applicable) | ☐ | |
| Token storage on device (secure storage?) | ☐ | |
| Certificate pinning | ☐ | |
| PII in logs | ☐ | |
| Object-level authorization (IDOR protection) | ☐ | |
| File upload validation | ☐ | |
| Dependency vulnerability scanning | ☐ | |
| Admin endpoints protected | ☐ | |
| Webhook signature verification | ☐ | |

| Question | Answer |
|---|---|
| Has a security review ever been done? | |
| Any known vulnerabilities currently accepted? | |
| Is there PII/financial data stored? What? | |
| Are there compliance requirements? | |

---

# 25. Testing Strategy

| Test type | Exists? | Count | Coverage | Runs in CI |
|---|---|---|---|---|
| Backend unit | ☐ | | | ☐ |
| Backend integration | ☐ | | | ☐ |
| API/e2e | ☐ | | | ☐ |
| Flutter unit | ☐ | | | ☐ |
| Flutter widget | ☐ | | | ☐ |
| Flutter integration | ☐ | | | ☐ |
| Load / performance | ☐ | | | ☐ |

| Question | Answer |
|---|---|
| How is a change verified before release? | |
| Is there a manual QA pass? | |
| Has load testing ever been run? Results? | |
| Is there a staging environment with production-like data? | |

```
<!-- paste one representative test -->
```

---

# 26. Performance Evidence & Measurements

> This section collects observations only. Do not interpret them here.

### 26.1 Where slowness is observed

| Area | Slow? | Cold start only? | Consistent? | Perceived duration |
|---|---|---|---|---|
| App cold launch | ☐ | ☐ | ☐ | |
| Splash → first screen | ☐ | ☐ | ☐ | |
| Login | ☐ | ☐ | ☐ | |
| OTP verify | ☐ | ☐ | ☐ | |
| Home screen load | ☐ | ☐ | ☐ | |
| Product list | ☐ | ☐ | ☐ | |
| Product detail | ☐ | ☐ | ☐ | |
| Images appearing | ☐ | ☐ | ☐ | |
| Cart operations | ☐ | ☐ | ☐ | |
| Subscription update | ☐ | ☐ | ☐ | |
| Calendar | ☐ | ☐ | ☐ | |
| Delivery logs | ☐ | ☐ | ☐ | |
| Reports | ☐ | ☐ | ☐ | |
| Notifications | ☐ | ☐ | ☐ | |
| Livestream | ☐ | ☐ | ☐ | |
| Scrolling smoothness | ☐ | ☐ | ☐ | |
| Navigation between tabs | ☐ | ☐ | ☐ | |

### 26.2 Endpoint timing measurements

> Run against production from a normal network. Repeat each 5 times; report median.

```bash
curl -w "dns:%{time_namelookup} tcp:%{time_connect} tls:%{time_appconnect} ttfb:%{time_starttransfer} total:%{time_total} size:%{size_download}\n" -o /dev/null -s '<url>'
```

| Endpoint | DNS | TCP | TLS | TTFB | Total | Size |
|---|---|---|---|---|---|---|
| | | | | | | |

### 26.3 Cold vs. warm

| Measurement | First request after idle | Subsequent request |
|---|---|---|
| Health endpoint TTFB | | |
| Home endpoint TTFB | | |

| Question | Answer |
|---|---|
| Does the Railway service sleep / scale to zero? | |
| Is there a warm-up or keep-alive ping? | |
| How long is "idle" before slowness returns? | |

### 26.4 Resource utilization

> From Railway metrics, over 7 days.

| Metric | Idle | Average | Peak |
|---|---|---|---|
| CPU | | | |
| Memory | | | |
| Network in | | | |
| Network out | | | |
| DB CPU | | | |
| DB connections | | | |

### 26.5 Client-side measurements

| Metric | Value | How measured |
|---|---|---|
| Time to first frame (release) | | |
| Time to home screen usable | | |
| Number of API calls on cold launch | | |
| Total bytes downloaded on cold launch | | |
| Are launch API calls parallel or sequential? | | |

### 26.6 Comparative observations

| Question | Answer |
|---|---|
| Is it slow on WiFi and mobile data equally? | |
| Slow on all devices or only low-end? | |
| Slow for all users or specific regions? | |
| Was it ever fast? When did it change? | |
| Did anything change around that time (deploy, data growth, dependency bump)? | |
| Is the same backend fast when called via `curl` from a laptop? | |

---

# 27. Known Constraints & Pain Points

### 27.1 Constraints

| Constraint | Detail |
|---|---|
| Budget limits | |
| Vendor lock-in | |
| Team size / skills | |
| Deadlines | |
| Backward compatibility requirements | |
| Devices that must be supported | |
| Regulatory | |

### 27.2 Known technical debt

| # | Area | Description | Known since | Impact |
|---|---|---|---|---|
| 1 | | | | |

### 27.3 Open questions from the team

> Things you already suspect but have not verified. List them plainly.

```
<!-- paste -->
```

### 27.4 Out of scope

> Anything the auditor should not touch or recommend changing.

```
<!-- paste -->
```

---

# 28. Appendices

### A. Full backend source tree
```
<!-- paste -->
```

### B. Full Flutter source tree
```
<!-- paste -->
```

### C. Complete `pubspec.lock`
```
<!-- paste -->
```

### D. Complete backend lockfile
```
<!-- paste -->
```

### E. Full database schema dump
```
<!-- paste -->
```

### F. Raw production log sample (extended)
```
<!-- paste -->
```

### G. Screenshots index

| ID | Description | File |
|---|---|---|
| S1 | Railway project canvas | `docs/audit/railway-canvas.png` |
| S2 | Railway service settings | `docs/audit/railway-settings.png` |
| S3 | Railway metrics, 7 days | `docs/audit/railway-metrics.png` |
| S4 | Database metrics | `docs/audit/db-metrics.png` |
| S5 | DevTools network, cold launch | `docs/audit/devtools-network.png` |
| S6 | DevTools performance, home scroll | `docs/audit/devtools-performance.png` |
| S7 | DevTools memory | `docs/audit/devtools-memory.png` |
| S8 | Crashlytics dashboard | `docs/audit/crashlytics.png` |
| S9 | Firebase Performance dashboard | `docs/audit/firebase-perf.png` |
| S10 | Architecture diagram | `docs/audit/topology.png` |

### H. Glossary of domain terms

| Term | Meaning |
|---|---|
| | |

---

## Sign-off

| Role | Name | Date | Confirmed complete |
|---|---|---|---|
| Author | | | ☐ |
| Reviewer | | | ☐ |

**Sections marked N/A:** _<!-- list them, so the auditor knows they were considered and not skipped -->_
