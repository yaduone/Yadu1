// Admin module is handled through auth routes for login.
// This file exists for potential future admin management features.
const express = require('express');
const router = express.Router();

module.exports = router;
